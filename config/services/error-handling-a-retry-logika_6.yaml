# Configuration for S-1010: Error handling a retry logika
# Part of Anonymizacni pipeline

service:
  name: "anonymizacni-pipeline"
  version: "1.0.0"

settings:
  enabled: true
  log_level: INFO
  max_retries: 3
  timeout_seconds: 30

database:
  pool_size: 5
  max_overflow: 10
