# Configuration for S-1003: K-anonymity validátor
# Part of K-anonymity engine

service:
  name: "k-anonymity-engine"
  version: "1.0.0"

settings:
  enabled: true
  log_level: INFO
  max_retries: 3
  timeout_seconds: 30

database:
  pool_size: 5
  max_overflow: 10
