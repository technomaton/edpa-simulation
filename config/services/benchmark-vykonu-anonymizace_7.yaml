# Configuration for S-1007: Benchmark výkonu anonymizace
# Part of L-diversity rozšíření

service:
  name: "l-diversity-rozsireni"
  version: "1.0.0"

settings:
  enabled: true
  log_level: INFO
  max_retries: 3
  timeout_seconds: 30

database:
  pool_size: 5
  max_overflow: 10
