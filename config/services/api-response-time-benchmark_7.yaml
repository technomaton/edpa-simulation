# Configuration for S-2207: API response time benchmark
# Part of Performance optimalizace

service:
  name: "performance-optimalizace"
  version: "1.0.0"

settings:
  enabled: true
  log_level: INFO
  max_retries: 3
  timeout_seconds: 30

database:
  pool_size: 5
  max_overflow: 10
