# Configuration for S-1204: Vocabulary lookup API
# Part of OMOP vocabulář služba

service:
  name: "omop-vocabular-sluzba"
  version: "1.0.0"

settings:
  enabled: true
  log_level: INFO
  max_retries: 3
  timeout_seconds: 30

database:
  pool_size: 5
  max_overflow: 10
