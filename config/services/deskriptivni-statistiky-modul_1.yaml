# Configuration for S-2001: Deskriptivní statistiky modul
# Part of Statistický engine

service:
  name: "statisticky-engine"
  version: "1.0.0"

settings:
  enabled: true
  log_level: INFO
  max_retries: 3
  timeout_seconds: 30

database:
  pool_size: 5
  max_overflow: 10
